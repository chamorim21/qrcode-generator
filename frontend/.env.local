NEXT_PUBLIC_CLIENT_URL=http://localhost:3000

# There are two API URls because the Next client and server look for them differently when it's running using Docker.
# However, running as production, they are the same.
NEXT_PUBLIC_API_URL_FROM_CLIENT=http://localhost:8000   
NEXT_PUBLIC_API_URL_FROM_SERVER=http://buzzvel-api:8000